{% extends 'base.html.twig' %}

{% block title %}CandidaturesLocations index{% endblock %}

{% block body %}
    <h1>CandidaturesLocations index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_complet</th>
                <th>Email</th>
                <th>Telephone</th>
                <th>Statut</th>
                <th>Date_location</th>
                <th>Duree_location</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for candidatures_location in candidatures_locations %}
            <tr>
                <td>{{ candidatures_location.id }}</td>
                <td>{{ candidatures_location.nomComplet }}</td>
                <td>{{ candidatures_location.email }}</td>
                <td>{{ candidatures_location.telephone }}</td>
                <td>{{ candidatures_location.statut }}</td>
                <td>{{ candidatures_location.dateLocation ? candidatures_location.dateLocation|date('Y-m-d') : '' }}</td>
                <td>{{ candidatures_location.dureeLocation }}</td>
                <td>
                    <a href="{{ path('app_candidatures_locations_show', {'id': candidatures_location.id}) }}">show</a>
                    <a href="{{ path('app_candidatures_locations_edit', {'id': candidatures_location.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_candidatures_locations_new') }}">Create new</a>
{% endblock %}
