<form method="post" action="{{ path('app_candidatures_locations_delete', {'id': candidatures_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidatures_location.id) }}">
    <button class="btn">Delete</button>
</form>
