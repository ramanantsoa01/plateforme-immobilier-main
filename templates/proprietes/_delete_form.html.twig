<form method="post" action="{{ path('app_proprietes_delete', {'id': propriete.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ propriete.id) }}">
    <button class="btn">Delete</button>
</form>
